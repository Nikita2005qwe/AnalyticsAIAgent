Документация к программе AI агента для помощи Аналитику

1. Точка входа в программу

Точкой входа является файл main.py, расположенный в корне проекта.
Он запускает графическое приложение через вызов Application().run().
Для запуска выполните:

```bash
python main.py
```

При старте приложение инициализирует GUI, настраивает вкладки и переходит в режим ожидания действий пользователя.
В случае ошибки запуска проверьте установку зависимостей и структуру проекта. 


2. Модули и классы
2.1 application (src/core/application.py)
Класс Application — основа графического интерфейса AI-агента.
Реализован на базе PyQt5 и отвечает за:

-Создание главного окна
-Настройку вкладок и элементов интерфейса (кнопки, поля ввода, лог)
-Обработку пользовательских действий:
-Создание торговой точки (одной и массово)
-Перепривязку ТТ к новому ESR
-Вывод результатов и ошибок в лог

Основные компоненты:
-Вкладки: Анализ, Управление ТО, Загрузка данных (по мере расширения)
-Поля ввода: ID, название, адрес, ESR, списки для массовых операций
-Обработчики событий: _on_create_single, _on_reassign_single, _on_bulk_reassign и др.
-Интеграция: Работает с PointManager из src/business/point_manager.py

Зависимости:
-PyQt5 — для GUI
-src.business.point_manager.PointManager — для выполнения бизнес-операций

Методы:
_on_create_single - код для обращения к классам которые принимают на себя реализацию бизнес логики для создания одной ТТ
_on_bulk_create - код для обращения к классам, которые принимают на себя реализацию процесса массового создания ТТ

2.2 poms объекты 

# Описать то какие объекты будем использовать для каких операций потребуются.

# Пронумеровать операции, оторые будем писать и проверить полное покрытие данных операций
ИИ агент будет действовать в 3 системах dms, CRM, 1С.
dms - система в браузере, структура которой описана в src/pages/dms
Включает в себя авторизацию, вход в панель дистрибьютора, навигацию по разделам с документами и прочим.
Протестируем для начала сайт, чтобы научиться выполнять на нём автоматические действия и сверять накладные.

Процесс сверки:
Запуск браузера, зайти на сайт good food shop, авторизация, вход в панель дистрибьютора, переход в раздел документы.
После данных действий программа выбирает последовательно каждое из существующих на данном аккаунте направление деятельности, проверяя через поиск и ожидание наличие накладных.
По итогам итерации происходит подсчёт статистики, сколько накладных выгруженно, а сколько не имеет данных в dms.
Список невыгруженных накладных является результатом данной операции.

2.3 слой с бизнес логикой
В данном модуле будут располагаться функции, которые делает аналитик, а именно те которые AI агент будет вызывать тогда, когда последует команда от пользователя.
Общий паттерн:
operation.py - содержит операцию для того чтобы сделать одну проверку, будет содержать низкоуровневые инструкции на основе selinium из модуля poms, а так же в будущем операции из модуля
интеграции с сервером 1С.
process.py - содержит функционал для полного осуществления бизнес процесса, будет инициализирован в ходе исполнения программы AI агентом. должен содержать метод run для запуска процесса,
метод validate - для валидации входных данных, метод load_data - для подгрузки данных которые необходимы для данного процесса, метод get_results, для получения результатов и обработки на корректность.

check_invoices - процесс проверки накладных
входные данные - список накладных
выходные данные - статистика по загруженным накладных. не загруженным накладным и накладным требующим ручной проверки.


3. Модели данных
Модели данных описывают структуру информации, с которой работает AI-агент.
Все данные изначально поступают из информационной базы 1С и представлены в формате JSON.
В будущем планируется прямая интеграция с 1С для чтения и записи.
На текущем этапе данные хранятся в виде готовых JSON-файлов, которые используются как источник для создания торговых точек в CRM.

3.1. TradePoint (торговая точка)
Базовая модель, представляющая торговую точку.
Создаётся на основе данных из 1С. Используется для массового создания ТО в CRM через автоматизацию.

📄 Поля модели (обязательные)

⚠️ Все поля — обязательные.
Необязательные поля в JSON не заполняются и не передаются. 

📦 Пример JSON
```json
{
    "Контрагент": "333 ИП Екешева Э.С.",
    "Адрес": "649000, Алтай Респ, Горно-Алтайск г, Вербицкого пер, дом № 25, корпус 1",
    "Тип ТТ": "counter",
    "Специализация": "весь ассортимент",
    "Местонахождение": "Street",
    "Площадь": "1-50",
    "код территории привязки": "E026AD7C-3246-E911-811D-005056011415",
    "Название сети": "NO CHAIN"
}
```

📁 Источник данных
Формат: JSON
Происхождение: выгрузка из 1С (информационная база предприятия)
Расположение: локальные файлы (на старте), позже — прямое API-взаимодействие с 1С

4. Тест-кейсы для системы

4.1 Тесты для poms
# описать тест-кейсы для poms
4.2 Тест-кейсы для операций